(require (lib "turtles.ss" "graphics"))

(define (cas-de-base a) (begin (draw a)))
(define (
                              
                              
                              
                              